<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="auto">

    <queries>
        <intent>
            <action android:name="org.ligi.gobandroidhd.ai.gnugo.GnuGoService" />
        </intent>
    </queries>

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true" /> <!-- Permission to keep the screen awake -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:logo="@mipmap/ic_launcher"
        android:requestLegacyExternalStorage="true"
        android:theme="@style/base_theme">
        <activity
            android:name="io.github.karino2.paoogo.ui.GameStartActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <uses-library
            android:name="com.sec.android.app.multiwindow"
            android:required="false" />

        <activity android:name=".ui.gnugo.PlayAgainstGnuGoActivity" />
        <activity android:name="io.github.karino2.paoogo.ui.vs_engine.PlayAgainstEngineActivity" />
        <activity android:name=".ui.BaseProfileActivity" />
        <activity android:name=".ui.editing.EditGameActivity" />
        <activity android:name=".ui.recording.GameRecordActivity" />
        <activity android:name=".ui.scoring.GameScoringActivity" />
        <activity android:name=".ui.GoPrefsActivity" />
        <activity android:name=".ui.GoActivity" />
    </application>

</manifest>